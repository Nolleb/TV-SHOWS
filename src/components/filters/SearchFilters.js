import React from "react";

const SearchFilters = () => (
  <div className="test">
    <div className="c-flex-container">
      <button className="c-btn">Movies</button>
      <button className="c-btn c-btn--inverted">TV shows</button>
    </div>
  </div>
);

export default SearchFilters;
