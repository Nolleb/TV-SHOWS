import React from "react";

const PageNotFound = () => (
  <div>
    <h2>Tv show - page not found</h2>
    <p>
      This app uses React, Redux, React Router, and many other helpful
      libraries.
    </p>
  </div>
);

export default PageNotFound;
